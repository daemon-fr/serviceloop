# V16 Service UI/UX Reference v1.0

This bundle preserves the owner-adopted presentation manual, mechanical token/coverage companions, and three visual reference images. The manual is canonical; the former self-contained HTML copy is retired. The review mockup with an obsolete standalone file suffix was removed.

The bundle's static counts describe design inventory, not runtime tests or app coverage. See `../../project/SOURCE_OF_TRUTH.md` for current authority and `../../project/IMPLEMENTATION_STATE.md` for branch/evidence.

Files: `V16Service_UI_UX_Implementation_Reference_v1_0.md`, token JSON, coverage CSV, current package audit/manifest, and `references/mockup_editor.png`, `mockup_outbox.png`, `mockup_selected.png`.
