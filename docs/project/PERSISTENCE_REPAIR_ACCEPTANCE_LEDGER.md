# Persistence repair acceptance ledger

This ledger belongs to the isolated `codex/persistence-web-foundation-audit` repair branch. Its base is B049 UI pass `13552b917d62a453f1840a5a8121742c9b11e420`. It records implemented contracts, observed checks, and remaining acceptance work. It does not designate this branch as accepted or authorize a protected-branch merge.

## Version and authority

| Contract | Audited | Repair target |
| --- | ---: | ---: |
| Room | 19 | 20, additive 19→20 |
| Recovery schema | 18 | 19, reads 9…19 |
| Encrypted Recovery container / `.slsync` envelope / DATA_TRANSFER metadata | 2 | 2 |
| WORK_RESULT / PERFORMED_WORK | 1 | emit 2, read supported 1 and 2 |
| Other DATA_TRANSFER families | 1 | 1 |
| FULL_WORKSPACE register / Dispatch | 3 / 5 | unchanged, existing legacy readers retained |
| App version name/code | 1.3.0 / 5 | unchanged |

The four Room 20 nullable columns are `final_work_items.followUpsSnapshotJson`, `remote_final_results.sourcePayloadJson`, `transferred_final_results.sourcePayloadJson`, and `retained_images.originalDeletionRequestedAtEpochMillis`. The receipt/result unique indexes now include author identity. Null source/follow-up snapshots mean unavailable legacy history; they are not an empty captured history. The `SourceCanonicalJsonTest` fixture pins UTF-8 canonical bytes and a SHA-256 digest for a v2 result record. Its fixed package ID/time are test inputs only; production package/section IDs remain random.

## R/F/D crosswalk and evidence

`IMPLEMENTED` describes code present; `TESTED` names an executed check; `DOMAIN-INSTRUMENTED` means production services in an isolated Android database; `UI-INSTRUMENTED`, `SYSTEM-HANDOFF`, and `HUMAN/RENDERED` are distinct claims. An unmentioned route is **NOT RUN**. The earlier 573-test JVM suite predates R05/R07 and is historical only. Closeout evidence now includes the focused four-class JVM run (40 tests), the planned broad gate (579 JVM tests), debug and Android-test APK builds, lint, the artifact-matching R12 AVD journey, and the two-test WORK_RESULT AVD class. Existing canonical AVD migration, native/result flows, bounded workload, system handoff and rendered PDF evidence remain applicable where their code was not changed. No local acceptance evidence gap remains; genuine historical-package execution remains unavailable as stated under R02.

| Repair / source findings | Implemented contract and source fixture | Executed evidence | Remaining gate |
| --- | --- | --- | --- |
| R01 / D01 | Room 14→15 stages FK closure before parent rebuild; Room 20 generated schema and 19→20 migration. | `RoomPersistenceRepairMigrationTest.populatedFourteenToFifteenPreservesEveryDependentChildAndBinding`, `populatedFourteenToTwentyPreservesEveryDependentChildAndBinding`, `eighteenToTwentyPreservesPopulatedRows`, `nineteenToTwentyKeepsRowsAndAddsNullableSnapshotsAndAuthorIndexes`, and `everySupportedSchemaStartHasAnExplicitPathToTwenty`: exact row/hash, FK, nullable-column and index assertions. Canonical AVD suite PASS (5 tests). | No migration-specific edit since this evidence; reuse it. Final shared-code build/device gate remains. |
| R02 / F01,D02 | Recovery 19 validates original source version/table shapes before normalization and accepts 9…19; authenticated ZIP must have a complete central directory. | `RecoveryLegacyCompatibilityTest.populatedRecoveryVersionsNineThroughNineteenRestoreExactBusinessValues`, `duplicateHistoricalTableFailsBeforeReplacingDurableRows`, `authenticatedZipTamperingAndTruncationRejectWithoutAdoption`, `malformedSourceVersionsFamiliesTypesAndAncestryRejectBeforeAdoption`, `orphanAggregateSourceAndRemotePhotoRejectBeforeReplacement`, and `B045SlsyncTest.invalidReferenceRollsBackAfterReplacementHasStarted`: projected values, reject-before-adoption and rollback assertions PASS. `RecoveryJournalInstrumentedTest.atomicJournalRestoresCommittedSnapshotOnDevice` PASS. | Genuine historical `.slbackup` package execution is unavailable; the projected 9…19 fixture is synthetic and is not historical-package evidence. |
| R03 / F02,D03 | Six durable owned roots, final-only photos, correction evidence, coalesced descriptors, owned-path checks, missing/corrupt rendition state. | `RecoveryLegacyCompatibilityTest.replacementRemovesUnownedBytesAcrossAllSixRootsButKeepsExternalFiles`, `sharedRenditionPathCoalescesOnlyWhenEveryDescriptorAgrees`, `incompleteBackupMarksMissingAggregateRenditionWithoutClaimingReadyBytes`, `corruptAggregateBytesAreQuarantinedAsIncompleteAcrossRepeatedBackup`; `B049ImageCleanupTest.cleanupDerivativeSurvivesCompleteRecoveryRoundTrip`, `interruptedDeletionReconcilesWithNeverPolicyAndBeforeCompleteBackup`, and `cleanupPreservesOriginalSharedWithWorkingEvidence`: hash/path, preservation, conflict and repeat-backup assertions PASS. Live AVD migration retained 65 table counts/hash comparisons and two owned files. | Reuse these assertions. Final shared-code build/device gate remains. |
| R04 / D04, D12 device | Appearance-only OS extraction rules; reminder/Calendar/cleanup binding uses dataset plus adoption token. | `OsBackupRulesTest.legacyFullBackupAllowsOnlyAppearance`, `cloudAndDeviceTransferEachAllowOnlyAppearance`, `ReminderDeviceBindingTest.requestRequiresCurrentDatasetAndAdoptionTokenAcrossReopenAndRollback`, and `CalendarIntegrationTest.sameDatasetAdoptionDropsCalendarBindingButRollbackKeepsIt`: allowlist and state-preservation/replacement assertions PASS; merged manifest inspected. System Sharesheet/Picker UI journey also passed. | Reuse this evidence; no remaining local assertion. The fake Calendar provider is explicitly not claimed as a live account/provider run. |
| R05 / F02,D05 | Shared business-file mutex, adoption journal, atomic Recovery moves, staged flush/verify, source DB recheck before encrypted backup, and interrupted original-deletion reconciliation. Native, readable, and WORK_RESULT exports capture Room reads in one transaction; JPEG derivation and ZIP/package encoding happen outside it. WORK_RESULT export validates count bounds before derivative preparation. | The four `BusinessFileAdoptionJournalTest` cases assert owned-byte cleanup/preservation, damaged-journal restriction, and startup reconciliation. `B049DataTransferRoundTripTest` and `B049WorkResultExportTest` close native/result behavior; all 40 tests in the focused closeout command PASS. Existing backup-race and image-deletion interruption tests PASS. | No product or local evidence gap remains; historical-package limitation is isolated to R02. |
| R06 / F03,D07 part | Canonical final-photo derivative stored once, verified and reused by result/native export; original remains separate. | `B049WorkResultExportTest.retainedDerivativeBytesStayIdenticalBeforeAndAfterOriginalRemoval`, `firstExportPersistsOneCanonicalDerivativeAcrossServiceRestartAndNativeRelay`; relay/Recovery hash assertions in `B049DataTransferRoundTripTest.performedWorkAndStandaloneImageArchiveStayImmutableAndRelayOriginalOrigin`; actual Android 12-photo result package/export/import/replay PASS. | Reuse existing byte-stability and AVD codec evidence; no new image journey is required. |
| R07 / F04,F06,D07 part | Revision-time follow-ups, versioned source snapshot adapters, v2 identity/chronology, complete native `sourceWorkSnapshot`, typed source references and canonical JSON. Shared projection v2 compares common public facts and compares private facts when both routes carry them. Identical v2 replay can restore a missing native source representation without new history effects. | `B049WorkResultExportTest.directPerformedAndWorkResultRelayHaveOneSourceFingerprint` checks public/private equivalence and rejects contradictory fully carried private facts; `B049DataTransferRoundTripTest.publicRelayOfPreviouslyPrivatePerformedWorkRedactsNestedWorkAndPhotos`, `identicalV2ReplayRecoversMissingLegacySourceRepresentation`; `B049WorkResultImportTest.exactReplayRecoversMissingV2SnapshotWithoutRepeatingBusinessEffects`; `SourceCanonicalJsonTest` PASS. The 40-test focused closeout run and 579-test broad gate pass after these edits. | No product or local evidence gap remains; historical-package limitation is isolated to R02. |
| R08 / F07,D06 | v2 semantic/outcome gates, typed checklist/findings/parts/follow-ups/photo descriptors, captured recurrence agreement and calculated/override decision; author/issuer/trust/assignment checks and once-only recurrence. | `SourceCanonicalJsonTest` rejects coerced source-photo identity and wrong numeric/boolean facts; `B049WorkResultImportTest.contradictoryResultSemanticsRejectBeforeAnyReceiptOrObligationEffect`, `staleResultIsRetainedWithoutRecurrenceAndWrongTargetIsRejected`, `canceledDispatchRetainsSourceWithoutCompletingVisitOrAdvancingPlan`, `independentAuthorsWithSameResultTokensBothRemainEffective`, `correctionChangingAppliedRecurrenceReportsItsCommittedConflict`; Android `exporterProducedV2PhotoResultIsAppliedAndReplayedWithoutEffects` PASS. | Reuse existing semantic, eligibility, and author assertions; no additional matrix required. |
| R09 / F05,F10,D07,D08 | Full photo descriptor replay comparison and committed-result feedback. | `B049WorkResultImportTest.mixedCommittedFeedbackUsesDurableStatusAndDoesNotCountReplayAsNewEffect` passes a mixed committed result through the same `workResultCommitMessage` renderer called by the import screen and asserts applied/stale/conflict/replay counts plus conflict detail; `B049DataTransferRoundTripTest.transferredEvidenceRetryComparesImmutableMetadataAndBytesWithoutMutationOnConflict`; Android `exporterProducedV2PhotoResultIsAppliedAndReplayedWithoutEffects` asserts applied then replayed without another effect. | Reuse the formatter-to-screen wiring and renderer assertion; no extra UI journey required. |
| R10 / D09 | Typed native entity keys, literal `null`, nullable refs, hierarchy/customer type and alias checks. | `B049DataTransferRoundTripTest.sameSourceIdAcrossNativeEntityTypesKeepsItsHierarchy`, `nativeExportChoosesOneStableAliasPairAndRejectsAmbiguousOrigins`, `literalNullFreeTextRemainsTextThroughNativeImportAndRetry`, `privacyOffNativeCustomerDataCarriesOperationalFieldsAndDetectsPhoneDrift`, `untrustedSourceAndConflictingBoundRecordCannotPartiallyWrite`: real Room mappings, classifications and no-partial-write assertions PASS. | Reuse these assertions; no extra collision/null test needed. |
| R11 / F02,D10 | Graph/source validation and explicit original/derivative/rendition availability. | `RecoveryLegacyCompatibilityTest.orphanAggregateSourceAndRemotePhotoRejectBeforeReplacement` checks LOCAL source/report owner and remote-photo/result owner rejection. New `polymorphicAggregateAndTransferredEvidenceOwnersRejectBeforeReplacement` asserts the exact error for orphan REMOTE and TRANSFERRED aggregate owners, transferred evidence linked to a missing result, and a rendition without a report; after each rejection, the durable customer row remains unchanged. Incomplete/corrupt rendition, malformed graph and `.slsync` rollback cases also PASS. | No remaining local graph assertion identified; no production edit was needed for this test-coverage gap. |
| R12 / F08,D10 part | Evidence/history reconciliation by source execution in either arrival order. | JVM `B049DataTransferRoundTripTest.evidenceAndPerformedHistoryAssociateInEitherArrivalOrder` checks both orders, retries, Recovery between stages, byte hashes, exact links and conflicting-link rejection. Artifact-matching AVD `B049DataTransferEndToEndInstrumentedTest.twoWorkspacesMergeSafeCurrentStateAndImmutableHistoryThenRelayOriginalOrigin` PASS (1 test): evidence→Recovery→history and history→Recovery→evidence both assert exact links and one report photo. | No remaining local evidence gap. |
| R13 / F09,D11,D12 chronology | Stable source/line identity, author-scoped effective chronology, frozen aggregate retry and rendition recovery. | `B049ImageCleanupTest.aggregateKeepsDistinctSameNameSiteWorkLines`, `equipmentScopedReportKeepsFrozenLineAfterReferenceEdit`; `B049WorkResultImportTest.twoLegacyRevisionsWithOnlyPackageTimeCannotChooseCurrentReportTruth`, `voidedLatestRemoteRevisionDoesNotResurrectAnOlderCurrentResult`, `aggregateGenerationReconcilesVerifiedAndIncompletePriorRenditions`; AVD `B049AggregatePdfInstrumentedTest.rendersTwoIdenticalServiceLabelsAsDistinctAttributedLines` was rendered and visually checked. | Reuse completed chronology/PDF evidence; no remaining assertion identified. |
| R14 / D12 | CSV escaping, business-zone bounds, coherent exports, bounded scale and canonical fixtures. | `B049WorkResultImportTest.readableCsvEscapesFormulaLikeTextAndRetainsQuotedUnicodeAndTypedNumbers`, `readableContactNoteDateBoundsUseInclusiveBusinessZoneDays`; native scope/privacy/date/revision and v2 golden tests; AVD 250-branch/120-booking and 12-photo workloads already PASS. Final `:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug --offline` gate PASS: 579 tests, zero failures/errors/skips; both APK tasks and lint PASS. | No core local gate remains. Extreme metadata/byte limits remain intentionally unallocated and are documented below. |

The existing `B049SystemHandoffUiTest` passes as a two-test suite on the canonical AVD after updating its stale output-format selector and scrolling the lazy Export Center before interacting. It exercised an actual Android picker cancellation, actual Sharesheet, and a generated native file selected through DocumentsUI. It previews the import; it does not claim an applied UI import or aggregate retry handoff. The first attempt failed from a stale selector/lazy child and was repaired in the test harness; no production UI change was made for that failure. After the final production APK install, migration/result/Recovery suites passed together (8 Android tests), the two handoff UI tests and bounded workload passed, and the native end-to-end test passed after correcting its stale root-level photo fixture to the owned `attachments/` root. The latter initial failure was test setup, not a production-file exception on a valid path.

## Representative Android observations

All figures are one bounded run on the dynamically resolved `Pixel 10a ServiceLoop` AVD (Android instrumentation; isolated in-memory Room databases and cache roots), not a universal performance claim. The physical phone connected to adb was unused. Existing AVD app data was preserved across explicit `assembleDebug` then `adb -s <resolved-serial> install -r` installs; no uninstall, clear, reset or AVD replacement was done.

| Fixture | Result |
| --- | --- |
| 12 JPEGs from actual Android `Bitmap` codec, v2 exporter→importer→replay | 664,007-byte package; export 456 ms; import 117 ms; used heap 5,182,144 bytes; max heap 201,326,592 bytes; 12 stored photo hashes verified. |
| 250 Customer/Site/Equipment/Plan branches, 250 obligations, 120 Booked Visits/WorkItems | setup 593 ms; native Customer Data export 4,073 ms / 13,419 bytes; readable export 229 ms / 9,503 bytes; used heap 11,489,984 bytes. |
| Two imported v2 work results, later correction, real aggregate PDF plus native/readable export | PDF 65 ms / 47,575 bytes; native 50 ms / 2,527 bytes; readable 24 ms / 1,860 bytes; used heap 8,114,880 bytes. |

Large maximum-size archive/photo/row limits were not allocated on the preserved AVD. Metadata-only count boundaries for WORK_RESULT results/photos and DATA_TRANSFER evidence entries were tested on the JVM; byte-size, expanded-ZIP and million-row extremes were not allocated. The visual PDF QA file was generated by Android, pulled to ignored build output, rendered with Poppler, and inspected: both `Annual service` lines and Alice/Bob attribution are legible with no clipping. The generated AVD QA file was removed after inspection. This visual check is separate from actual UI and Android Sharesheet evidence.

## Persistent store and portability matrix

| Store / owner | Content and authority | Recovery / replacement behavior |
| --- | --- | --- |
| `serviceloop.db` / `ServiceLoopDatabase` | Room is authoritative for directory, plans/obligations, drafts, final revisions, receipts, transfer provenance, rendition descriptors, retained-image markers and Recovery metadata. | Encrypted Recovery and FULL_WORKSPACE have distinct replacement contracts; DATA_TRANSFER adds selected content only. |
| `filesDir/{attachments,reports,retained-images,remote-results,aggregate-reports,transferred-evidence}` / `OwnedBusinessFiles` | Durable app-owned binary evidence and reports, referenced from Room. | Included by explicit descriptor inventory; complete backup fails on missing required bytes; erase/replacement touches only these roots. |
| `filesDir/recovery` / `RecoveryPackage` | Stage, rollback and atomic journal; operational commit recovery, not business history. | Excluded from portable/OS backup. A damaged or ambiguous journal restricts recovery. |
| `shared_prefs/serviceloop_appearance.xml` / `AppearancePreferences` | Harmless device appearance. | Only ordinary OS extraction allowlisted item; not business Recovery truth. |
| `shared_prefs/dispatch_prototype.xml` / role/office settings | Local role capability choice and office report-recipient setting. | Device local; role is not imported server authorization. |
| `shared_prefs/serviceloop_ui_filter_preferences.xml`, `serviceloop_search_ui_preferences.xml` | Local filter/search convenience. | Device local, not portable business truth. |
| `shared_prefs/serviceloop_reminder_device.xml` / `ReminderCoordinator` | Notification request/suppression and device binding. | Device local; dataset/adoption mismatch turns stale requests off. |
| `noBackupFilesDir/calendar-integration.json` / `CalendarDeviceStore` | Android provider event links and selected calendar. | Device local; replacement disables, existing external events are not erased. |
| `shared_prefs/serviceloop_image_cleanup.xml` / `ImageCleanupService` | Destructive retention choice, binding and last run. | Device local; unmatched/replaced dataset defaults Never and requires reconfirmation. |
| Android URI grants, picker/share/export cache | External destination permission or disposable handoff state; never domain authority. | Not portable; verified-backup metadata changes only after write/readback/inspection succeeds. |

## Portable fixture and adapter notes

### Frozen v2 source field map

| Meaning | WORK_RESULT v2 record | PERFORMED_WORK v2 record | Public native view |
| --- | --- | --- | --- |
| Author and execution | `originWorkspaceId`, `sourceVisitId`, `sourceWorkItemId`, `sourceWorkItemPosition` | Same source keys, retained across a Coordinator relay | Retained |
| Revision and order | `sourceFinalRevisionId`, `sourceFinalRevisionNumber`, `supersedesSourceFinalRevisionId`, `recordedAt`, `correctionReason`, `publicNote` | Same source keys; package time and local import time do not replace `recordedAt` | Retained; nullable lineage stays null |
| Source directory references | `sourceCustomerRef`, `sourceSiteRef`, nullable `sourceEquipmentRef` | Same typed origin/entity pairs; local `customer`, `site`, `equipment` links are transfer hints | Source refs retained; receiver-local hints excluded from immutable equality |
| Dispatch assignment | `resultId`, issuer, Dispatch Visit/item, generation and material hash | `assignmentProvenance` where frozen by the source; `logicalResultId` is a link hint | Retained where present |
| Captured subject and people | `visitReference`, `serviceDate`, technician ID/name/designation, Customer/Site/subject snapshots | Same fields, with service name and outcome at record level | Retained |
| Work and recurrence | `workSnapshot` with service/plan/public work/reason, fulfillment, captured obligation, old/next due, calculation/override and work private note; `recurrence` separately | `sourceWorkSnapshot` plus flat projected work fields and `recurrence`; duplicated facts must agree | `sourceWorkSnapshot.privateInternalNote` and flat `internalNotes` removed |
| Revision private note | `privateInternalNote`, separate from work private note | `finalInternalNote`, separate from work `internalNotes` | Removed, including relays imported with private content |
| Ordered answers | `checklist`, derived `findings`, `parts` | Same arrays, preserving source positions and quantity strings | Retained |
| Follow-ups | `followUpCaptureState`, frozen `followUps` from revision-time snapshot | Same capture state and records; SQL-null local legacy snapshot means unavailable | Private planning note removed; unavailable remains distinct from captured empty |
| Source media | `sourcePhotos` original identity, position/hash/size/MIME, caption, visibility, report inclusion and correction provenance | Same source facts independently of selected EVIDENCE bytes | Only PUBLIC source photos; binary derivative is separate EVIDENCE with its own hash/dimensions |

The accepted source record is stored in a versioned `FinalSourceSnapshot` wrapper. The wrapper is not an external package. Native re-export reads the saved semantic record; projected Room columns support indexing and reports. Versioned immutable comparison excludes transfer-time local links and binary section names, compares shared public facts across privacy views, and compares private facts when both representations retain them. A public relay may match a complete private source without claiming redacted fields are empty; two complete representations with different private facts conflict. Exact original-package replay recovers a missing legacy source snapshot without replaying business effects.

`app/src/test/resources/contracts/work-result-v2-record.json` is synthetic and contains fixed valid technician IDs, separate source/assignment/revision namespaces, a corrected revision lineage, date-only service date versus UTC recorded instant, null public note, distinct work/revision private notes, string decimal quantity, a Unicode Customer name, and explicit known-empty photo/follow-up arrays. `work-result-v2-canonical.json` is the exact UTF-8 key-ordered byte sequence, without BOM or whitespace, and `work-result-v2.sha256` pins its digest. `SourceCanonicalJsonTest` verifies bytes, digest, accepted v2 codec round trip and a rejected `DONE` mutation. No private owner data is in these files.

Historical Recovery evidence limitation: no genuine 9–18 `.slbackup` package was supplied or is present in the repository fixtures. `RecoveryLegacyCompatibilityTest.populatedRecoveryVersionsNineThroughNineteenRestoreExactBusinessValues` constructs synthetic versioned projections and proves modeled normalization/restore behavior only. It does not establish compatibility with a package created by an historical application build. This evidence is recorded as unavailable; the synthetic test is not credited as historical-package verification.

The fixture illustrates a semantic record, not a raw Room/API schema or a complete encrypted `.slsync` package. A future platform adapter must preserve source IDs, author namespace, revision lineage, nullable/omitted/redacted meaning, original photo facts separately from derivative transport bytes, and the published package versions. It must treat an old null `sourcePayloadJson` as unavailable and require exact source replay before claiming a lossless v2 relay. No web runtime, backend, account model or sync protocol is chosen here.
